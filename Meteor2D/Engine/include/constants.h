#pragma once
#include <meteorutils/color.h>
namespace meteor {

	//todo:
	// move these project property file.
	const float LOGICAL_WIDTH =  1920;
	const float LOGICAL_HEIGHT = 1080;

	//todo:
// move these values to an application property file.
	const size_t SCREEN_WIDTH = 1280;
	const size_t SCREEN_HEIGHT = 720;

	const bool FULLSCREEN = false;
	const bool USE_VSYNC = true;

	const Color BG_COLOR = Color::black();
}